# Copy this file to `.env` to customize the local login credentials.
STREAMLIT_UI_USERNAME=student
STREAMLIT_UI_PASSWORD=streamlit-demo

# Optional: point the UI to a different FastAPI deployment.
# AGENT_API_BASE=http://localhost:8000
